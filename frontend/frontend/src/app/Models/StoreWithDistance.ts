// import { Store } from "./Store";

export class StoreWithDistance {
    // id: number;
    // store: String;
    // distance: number;
    storeName: string;  // Update the class property name to storeName
    gstId: number;
    distance: number;
}